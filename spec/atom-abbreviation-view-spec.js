'use babel';

import AtomAbbreviationView from '../lib/atom-abbreviation-view';

describe('AtomAbbreviationView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
